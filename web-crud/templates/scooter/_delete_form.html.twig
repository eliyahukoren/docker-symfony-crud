<form method="post" action="{{ path('scooter_delete', {'id': scooter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <button class="btn btn-outline-success float-right">Delete</button>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scooter.id) }}">
</form>
